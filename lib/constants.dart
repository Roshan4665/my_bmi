import 'package:flutter/material.dart';

const kActive = Color(0xFF1D1E33);
const kInactive = Color(0xFF111328);
const kNumLayout = TextStyle(
  fontSize: 40,
  fontWeight: FontWeight.w900,
);

const gr =
    'You did not select any gender before starting the Calculation. To get gender specific result please select your gender before Starting.';

const gr2 =
    'For adults, BMI classifications do not depend on age or gender. For children and adolescents between 2 and 20 years old, BMI is interpreted relative to a child\'s age and gender, because the amount of body fat changes with age and varies by gender.';

const ar =
    'BMI is used differently for children. It is calculated in the same way as for adults but then compared to typical values for other children of the same age. Instead of comparison against fixed thresholds for underweight and overweight, the BMI is compared against the percentiles for children of the same gender and age.';
